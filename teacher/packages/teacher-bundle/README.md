# @teacher-dsh/teacher-bundle

Teacher DSH 的 profile bundle，只负责：

- 注册 bundled teacher skills 目录
- 设置 teacher toolchain path（teacher-artifact）
- 设置模板目录
- 设置 permission 默认值为 `workspace-write`

不把 Three.js 之类变成 DSH Plugin。