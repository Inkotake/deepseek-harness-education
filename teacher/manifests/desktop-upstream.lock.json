{
  "name": "desktop-upstream.lock",
  "version": "0.1.0",
  "watches": "upstream pins only; this fork is not the subject of this file",
  "desktop": {
    "repo": "https://github.com/anywhere-labs/dsh-desktop",
    "commit": null,
    "note": "Upstream Desktop baseline. This fork pins the Harness (see `harness`), not an upstream Desktop commit, so `commit` stays null until an operator resolves a revision with --desktop-commit. The Desktop base version this fork derives from is recorded in THIRD_PARTY_NOTICES.md. This fork's own HEAD is never written here."
  },
  "harness": {
    "repo": "https://github.com/deepseek-ai/deepseek-harness.git",
    "pinned_by_desktop": true,
    "channel": "stable",
    "commit": "fb2c4b9e698e30edb738bca4cf0618587db7d203",
    "version": "0.1.5-rc.2"
  },
  "policy": "Upgrade only through PR + tests; never follow master/latest automatically."
}
